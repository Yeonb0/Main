---
aliases:
  - Struct Memory Layout
  - 구조체 메모리 레이아웃(Struct Memory Layout)
---

- [[Record Type|struct]] 의 각 [[변수]] -> [[필드]] ==(field)==
- 정의 자체는 변수 X -> [[데이터 타입]] 처럼 사용
- [[메모리]] 상 연속된 block [[배치 처리|배치]] -> 전체 size 는 compiler 결정
- 어셈블리에서는 [[이름]] X -> base + offset 접근

### 선언
```c
struct Student {
	int id;
	char name[MAXLEN];
	double GPA;
};

struct Student s, *ps;
struct Student CSE3030[150];

s.id = 20230123
ps = &s;
printf("Student ID: %d\n", ps->id);
```

### 배치
```c
struct node {
	int a[4];          // 4(int) * 4 
	long l;            // 8(long)
	struct node *next; // 8(pointer)
```

![[Assembly-Array-and-Struct-07.png]]

### 어셈블리 접근
```c
void set(struct node *n, int v, int idx) {
	while (n) {
		n->a[idx] = v;
		n = n->next;
	}
}
```
```nasm
0x401106 <+0>:  jmp 0x401112 <set+12>
0x401108 <+2>:  movslq %edx,%rax       ; %rax: 'idx'
0x40110b <+5>:  mov %esi,(%rdi,%rax,4) ; n->a[idx] = v;
0x40110e <+8>:  mov 0x18(%rdi),%rdi    ; n = n->next
0x401112 <+12>: test %rdi,%rdi
0x401115 <+15>: jne 0x401108 <set+2>   ; if (n) goto ...
0x401117 <+17>: ret
```
- `0x18` -> 24 ==(`next` 의 offset)==
