- `function` 키워드 이용해 함수 정의.
- 단, 함수의 이름을 반드시 정의하지 않아도 됨

```js
const hello = function() { // 표현
	console.log("hello");
}

hello(); // 호출
```

- [[익명 함수]] : `function` 키워드 다음 함수 이름 작성 X
- [[익명 함수]]를 할당한 [[변수]] `hello` 를 통해 호출
- [[함수 표현식]]으로 선언한 함수는 [[호이스팅]] X -> 선언 이후에만 호출 가능
