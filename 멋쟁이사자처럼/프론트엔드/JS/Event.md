---
aliases:
  - 이벤트
  - event
---
- 웹 페이지에서 발생하는 각종 사건 / 상호작용
- 이벤트 발생 시 브라우저가 신호를 만듬 -> 미리 등록한 코드 실행
  ![[Pasted image 20260703101659.png]]

- [[이벤트 리스너]] -> [[Event|이벤트]] 처리
- [[이벤트 객체]] -> [[이벤트 리스너]]에 [[매개 변수]]로 넘겨지는 정보
- [[이벤트 버블링]]
- [[이벤트 위임]]
- 

## 종류
### 마우스 이벤트
- 마우스를 이용한 사용자의 동작과 관련

| 이벤트         | 설명                                      |
| ----------- | --------------------------------------- |
| `click`     | 사용자가 마우스로 요소를 클릭할 때 발생 (버튼을 눌렀다가 떼었을 때) |
| `dblclick`  | 요소를 더블 클릭할 때 발생                         |
| `mouseover` | 마우스 커서를 요소 위에 올렸을 때 발생                  |
| `mouseout`  | 요소 위에 있던 마우스 커서가 밖으로 벗어났을 때 발생          |
| `mousedown` | 마우스 버튼을 누른 순간                           |
| `mouseup`   | 누른 마우스 버튼을 떼는 순간                        |
| `mousemove` | 마우스 커서의 움직임                             |

### 키보드 이벤트
- 키보드를 입력할 때 발생하는 이벤트
- [[Form]]에 글자 입력 / 게임에서 키 조작 처리시 활용

| 이벤트       | 설명                                    |
| --------- | ------------------------------------- |
| `keydown` | 사용자가 키보드를 눌렀을 때 발생 (키를 누르는 순간 한 번 발생) |
| `keyup`   | 눌렀던 키를 떼는 순간 발생                       |
- `event.key` : 사용자가 누른 key 값 알 수 있음

### 폼 이벤트
- [[Form]] 과 관련된 요소에서 발생하는 이벤트
- 사용자 입력 / 폼 제출 등 사용자 입력 양식에 특화된 이벤트

| 이벤트            | 설명                                                                                            |
| -------------- | --------------------------------------------------------------------------------------------- |
| `submit`       | 사용자가 폼을 제출할 때 발생  <br>(`**<form>**` 요소에서 사용, 폼 데이터 검증 및 전송 처리)                                |
| `change`       | 입력 필드(`**<input>**`, `**<select>**` 등)의 값이 변경될 때 발생  <br>(체크 박스 상태 변경, 텍스트 입력 후 포커스를 잃었을 때 등) |
| `focus / blur` | 입력 요소가 포커스를 받을 때(`focus`), 포커스를 잃을 때(`blur`) 발생 (UI에서 입력 필드 강조나 검증 메시지 표시할 때 사용)              |
