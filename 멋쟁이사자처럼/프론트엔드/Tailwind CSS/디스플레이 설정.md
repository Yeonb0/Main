- `flex`, `inline`, `inline-block`, `inline-flex` 제공

- [[Flexbox]] 적용 가능 속성
	- `justify-{속성}`
	- `items-{속성}`

```html
<div className="flex justify-between items-center bg-slate-100 mt-3">
  <div className="bg-red-50">나는 플렉스1</div>
  <div className="bg-yellow-50">나는 플렉스2</div>
</div>
```