---
aliases:
  - Co-occurrence Matrix
  - 동시발생 행렬(Co-occurrence Matrix)
---

- 각 단어의 맥락에 어떤 단어가 몇 번 등장했는지 집계한 행렬
- [[분포 가설]] 을 빈도로 구현 -> [[통계 기반 기법]] 의 출발점
- 행 하나 = 해당 단어의 벡터 표현

![[자연어와-단어의-분산-표현-04.png]]
- ==ex)== `'You say goodbye and I say hello.'` 의 동시발생 행렬

### 구현
```python
def create_co_matrix(corpus, vocab_size, window_size=1):
    '''동시발생 행렬 생성

    :param corpus: 말뭉치(단어 ID 목록)
    :param vocab_size: 어휘 수
    :param window_size: 윈도우 크기
    
    :return: 동시발생 행렬
    '''
    corpus_size = len(corpus)
    co_matrix = np.zeros((vocab_size, vocab_size), dtype=np.int32)

    for idx, word_id in enumerate(corpus):
        for i in range(1, window_size + 1):
            left_idx = idx - i
            right_idx = idx + i

            if left_idx >= 0:
                left_word_id = corpus[left_idx]
                co_matrix[word_id, left_word_id] += 1

            if right_idx < corpus_size:
                right_word_id = corpus[right_idx]
                co_matrix[word_id, right_word_id] += 1

    return co_matrix
```
- `co_matrix` : 어휘 수 × 어휘 수 크기의 ==0== [[배열]] 로 초기화
- 모든 단어에 대해 [[윈도우 크기]] 안 주변 단어 카운트
	- `left_idx >= 0` · `right_idx < corpus_size` -> [[말뭉치]] 경계 확인

### 한계
- 단순 등장 횟수 기반 -> 고빈도 단어와 강한 관련성으로 오판
	- ==ex)== `"car"` 가 `"drive"` 보다 `"the"` 와 더 관련 있다고 평가
- -> [[점별 상호정보량]] 으로 개선
- 원소 대부분 ==0== ([[희소벡터]]) -> [[차원 감소]] 필요
