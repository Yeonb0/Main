---
aliases:
  - 3-Layer Neural Network
  - 3층 신경망
source: 신경망.md
created: 2026-07-30
---

- [[넘파이]] 행렬로 [[머신러닝/신경망]] 순방향 신호 전달 구현

![[신경망-16.png]]

| 층 | 뉴런 수 |
| --- | --- |
| [[입력층]] 0층 | 2개 |
| 첫 번째 [[은닉층]] 1층 | 3개 |
| 두 번째 은닉층 2층 | 2개 |
| [[출력층]] 3층 | 2개 |

### 표기법
![[신경망-17.png]]
- $w^{(1)}_{12}$
	- $^{(1)}$ : 1층의 [[가중치]], 뉴런
	- $_{12}$ : 앞 층의 2번째 뉴런 ($x_2$) 에서 다음 층의 1번째 뉴런 ($a_1^{(1)}$) 으로 향할 때의 가중치 (← 방향)

#### 1. 입력층 -> 1층 신호 전달
![[신경망-18.png]]
- [[편향]] 뉴런 추가 -> 아래 인덱스가 하나뿐
- 수식 -> $a^{(1)} = w_{11}^{(1)}x_1 + w_{12}^{(1)}x_2 + b^{(1)}_1$
- [[행렬의 곱]] -> $\text{A}^{(1)} = \text{XW}^{(1)} + \text{B}^{(1)}$

![[신경망-19.png]]

```python
X = np.array([1.0, 0.5])
W1 = np.array([[0.1, 0.3, 0.5], [0.2, 0.4, 0.6]])
B1 = np.array([0.1, 0.2, 0.3])

print(W1.shape)
print(X.shape)
print(B1.shape)

A2 = np.dot(X, W1) + B1
print(A2)
```
![[신경망-20.png]]

#### 2. 1층의 [[활성화 함수]] 처리
![[신경망-21.png]]
- $a$ : 가중 신호 & 편향 총합
- $z$ : $h()$ 로 변환된 신호

```python
Z1 = sigmoid(A1)

print(A1)
print(Z1)
```
![[신경망-22.png]]

#### 3. 1층 -> 2층 신호 전달 & 활성화 함수 처리
![[신경망-23.png]]
- 입력 -> 1층과 동일하게 구현

```python
W2 = np.array([[0.1, 0.4], [0.2, 0.5], [0.3, 0.6]])
B2 = np.array([0.1, 0.2])

print(Z1.shape)
print(W2.shape)
print(B2.shape)

A2 = np.dot(Z1, W2) + B2
Z2 = sigmoid(A2)

print(A2)
print(Z2)
```
![[신경망-24.png]]

#### 4. 2층 -> 출력층 신호 전달
![[신경망-25.png]]
- 활성화 [[함수]]로 [[항등 함수]] $\sigma$ 사용

```python
def identity_function(x):
    return x

W3 = np.array([[0.1, 0.3], [0.2, 0.4]])
B3 = np.array([0.1, 0.2])

A3 = np.dot(Z2, W3) + B3
Y = identity_function(A3)

print(A3)
print(Y)
```
![[신경망-26.png]]
- 입력 = 출력 -> 굳이 쓰는 이유는 흐름 통일

### 구현 정리
```python
def init_network():
    network = {}
    network['W1'] = np.array([[0.1, 0.3, 0.5], [0.2, 0.4, 0.6]])
    network['b1'] = np.array([0.1, 0.2, 0.3])
    network['W2'] = np.array([[0.1, 0.4], [0.2, 0.5], [0.3, 0.6]])
    network['b2'] = np.array([0.1, 0.2])
    network['W3'] = np.array([[0.1, 0.3], [0.2, 0.4]])
    network['b3'] = np.array([0.1, 0.2])
    
    return network

def forward(network, x):
    W1, W2, W3 = network['W1'], network['W2'], network['W3']
    b1, b2, b3 = network['b1'], network['b2'], network['b3']
    
    A1 = np.dot(x, W1) + b1
    Z1 = sigmoid(A1)
    A2 = np.dot(Z1, W2) + b2
    Z2 = sigmoid(A2)
    A3 = np.dot(Z2, W3) + b3
    Y = identity_function(A3)
    
    return Y

network = init_network()
x = np.array([1.0, 0.5])
y = forward(network, x)
print(y)
```

> [!note]- 실행 결과
> ![[신경망-27.png]]

- `init_network()` : 가중치 & 편향 초기화 -> 딕셔너리 `network` 에 저장
- `network` : 각 층에 필요한 매개변수 (가중치 & 편향) 저장
- `forward()` : 입력 -> 은닉 -> 출력 처리 과정 구현

### 관례
- 가중치 -> 대문자 `W`
- 입출력 & 편향 & 중간 결과 -> 소문자 `x` , `y` , `b` , `z1`
