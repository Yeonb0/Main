---
aliases:
  - Overfitting
  - 오버피팅
  - 과적합(Overfitting)
  - 과대적합
---

- 한 데이터셋에만 지나치게 최적화된 [[state|상태]]
- unseen data 대응 X -> 범용 능력 ↓
- [[훈련 데이터]] 정확도 ↑ / [[시험 데이터]] 정확도 정체 or ↓

### 확인
- [[에포크]] 별 훈련 · 시험 정확도 기록 -> 추이 비교

```python
from mnist import load_mnist

(x_train, t_train), (x_test, t_test) = load_mnist(normalize=True, one_hot_label=True)

train_loss_list = []
# ------------ 추가 -------------
train_acc_list = []
test_acc_list = []
# -------------------------------

# 하이퍼 파라미터
iters_num = 10 # 반복 횟수
train_size = x_train.shape[0]
batch_size = 100 # 미니배치 크기
learning_rate = 0.1
network = TwoLayerNet(input_size=784, hidden_size=50, output_size=10) # 은닉층 50개로 설정

for i in range(iters_num):
    # 미니배치 획득
    batch_mask = np.random.choice(train_size, batch_size)
    x_batch = x_train[batch_mask]
    t_batch = t_train[batch_mask]

    # 기울기 계산
    grad = network.numerical_gradient(x_batch, t_batch)

    # 매개변수 갱신
    for key in ('W1', 'b1', 'W2', 'b2'):
        network.params[key] -= learning_rate * grad[key]

    # 학습 경과 기록
    loss = network.loss(x_batch, t_batch)
    train_loss_list.append(loss)

    # ------------ 1 에포크 당 정확도 계산 ------------
    if i % 1 == 0:
        train_acc = network.accuracy(x_train, t_train)
        test_acc = network.accuracy(x_test, t_test)
        train_acc_list.append(train_acc)
        test_acc_list.append(test_acc)
        print("train acc, test acc | " + str(train_acc) + ", " + str(test_acc))
    # -------------------------------------------------
```

![[신경망-학습-23.png]]
- 훈련 · 시험 [[데이터]] 정확도 추이
- 두 정확도 차이 X -> 과적합 X

### 발생 조건
- 매개변수 多 · 표현력 높은 모델
- [[훈련 데이터]] 적음
- ==ex)== [[MNIST]] 훈련 데이터 ==300개== + 7층 [[신경망]] (각 층 [[뉴런]] 100개, [[ReLU 함수]])

```python
from mnist import load_mnist
(x_train, t_train), (x_test, t_test) = load_mnist(normalize=True)
# 과적합 위해 학습 데이터 수 줄임
x_train = x_train[:300]
t_train = t_train[:300]
```

```python
network = MultiLayerNet(input_size=784, hidden_size_list=[100, 100, 100, 100, 100], output_size=10)
optimizer = SGD(lr=0.01) # 학습률이 0.01인 SGD 로 매개변수 갱신
max_epochs = 201
train_size = x_train.shape[0]
batch_size = 100

train_loss_list = []
train_acc_list = []
test_acc_list = []

iter_per_epoch = max(train_size / batch_size, 1)

epoch_cnt = 0
for i in range(1000000000):
    batch_mask = np.random.choice(train_size, batch_size)
    x_batch = x_train[batch_mask]
    t_batch = t_train[batch_mask]

    grads = network.gradient(x_batch, t_batch)
    optimizer.update(network.params, grads)

    if i % iter_per_epoch == 0:
        train_acc = network.accuracy(x_train, t_train)
        test_acc = network.accuracy(x_test, t_test)
        train_acc_list.append(train_acc)
        test_acc_list.append(test_acc)

        epoch_cnt += 1
        if epoch_cnt >= max_epochs:
            break
```

![[학습-관련-기술들-20.png]]
- train 정확도 ==거의 100%== / test 정확도 저조 -> 과적합

### 억제
- [[가중치 감소]]
- [[드롭아웃]]
- [[배치 정규화]]
