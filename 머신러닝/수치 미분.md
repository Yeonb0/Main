---
aliases:
  - Numerical Differentiation
  - 수치 미분(Numerical Differentiation)
---

- 해석적 미분을 근사치로 계산하는 방법
- 해석적 미분 : 수식 전개로 구하는 미분
- 미분 : 한 순간의 변화량

$$
\frac{df(x)}{dx} = \lim_{h \to 0} \frac{f(x+h) - f(x)}{h}
$$

- $x$ 의 작은 변화가 [[함수]] $f(x)$ 를 얼마나 변화시키는지
- 이때 변화를 0 에 한없이 가깝게 $\lim_{h \to 0}$

### 구현
```python
def numerical_diff(f, x):
    h = 1e-50 
    return (f(x + h) - f(x)) / (2 * h)
```
- 문제 2가지
	1. `h` 값 `1e-50` -> 반올림 오차 발생 -> $10^{-4}$ 정도 사용
	2. 함수 `f` 의 차분 -> 오차 발생 -> $(x + h)$, $(x - h)$ 의 차분 사용 (중심 차분 · 중앙 차분)

```python
# 개선 버전
def numerical_diff(f, x):
    h = 1e-4 # 0.0001
    return (f(x + h) - f(x - h)) / (2 * h)
```

### 예
$$
y = 0.01x^2 + 0.1x
$$

```python
def function_1(x):
    return 0.01 * x ** 2 + 0.1 * x
```

```python
x = np.arange(0.0, 20.0, 0.1)
y = function_1(x)
plt.xlabel("x")
plt.ylabel("f(x)")
plt.plot(x, y)
plt.show()
```
![[신경망-학습-07.png]]

- $x = 5$, $x = 10$ 일 때 미분 값
```python
print(numerical_diff(function_1, 5))
print(numerical_diff(function_1, 10))
```
![[신경망-학습-08.png]]
- 실제 미분값 ==0.2==, ==0.3== -> 오차 매우 작음

> [!note]- 접선 그리기
> ```python
> # 접선 그리기(x = 5)
> tf = tangent_line(function_1, 5)
> y2 = tf(x)
> 
> plt.plot(x, y)
> plt.plot(x, y2)
> plt.show()
> 
> # 접선 그리기(x = 10)
> tf = tangent_line(function_1, 10)
> y2 = tf(x)
> 
> plt.plot(x, y)
> plt.plot(x, y2)
> plt.show()
> ```
> ![[신경망-학습-09.png]]
