---
aliases:
  - Contexts and Target
  - 맥락과 타깃(Contexts and Target)
---

- [[머신러닝/신경망|신경망]] 입력 = 맥락 (context) / [[정답 레이블]] = 타깃 (target)
- 타깃 = 맥락에 둘러싸인 중앙 단어
- [[CBOW 모델]] · [[skip-gram 모델]] 의 학습 [[데이터]]

![[word2vec-18.png]]
- 목표 : 맥락 입력 시 타깃 출현 확률 ↑

### 절차
1. [[말뭉치]] 텍스트 -> 단어 ID 변환 : `preprocess()`
2. `create_contexts_target()` -> 맥락 & 타깃 생성
3. `convert_one_hot()` -> 원핫 표현 변환

### preprocess()
```python
import sys
sys.path.append('../')
from common.util import preprocess

text = 'You say goodbye and I say hello.'
corpus, word_to_id, id_to_word = preprocess(text)
print(corpus)

print(id_to_word)
```

> [!note]- 실행 결과
> ![[word2vec-19.png]]

### create_contexts_target()
```python
def create_contexts_target(corpus, window_size=1):
    '''맥락과 타깃 생성

    :param corpus: 말뭉치(단어 ID 목록)
    :param window_size: 윈도우 크기(윈도우 크기가 1 -> 좌우 한 단어)
    :return:
    '''
    target = corpus[window_size:-window_size]
    contexts = []

    for idx in range(window_size, len(corpus)-window_size):
        cs = []
        for t in range(-window_size, window_size + 1):
            if t == 0:
                continue
            cs.append(corpus[idx + t])
        contexts.append(cs)

    return np.array(contexts), np.array(target)
```

![[word2vec-20.png]]

```python
from common.util import create_contexts_target

contexts, target = create_contexts_target(corpus, window_size=1)

print(contexts)
print(target)
```

> [!note]- 실행 결과
> ![[word2vec-21.png]]

- 반환 값 여전히 단어 ID -> 원핫 표현 변환 필요

### convert_one_hot()
![[word2vec-22.png]]
- 원래 [[배열]] 펼치기 -> 형상 (6, 2) -> (6, 2, 7)
- 인수 : 단어 ID 목록 & 어휘 수

```python
def convert_one_hot(corpus, vocab_size):
    '''원핫 표현으로 변환

    :param corpus: 단어 ID 목록(1차원 또는 2차원 넘파이 배열)
    :param vocab_size: 어휘 수
    :return: 원핫 표현(2차원 또는 3차원 넘파이 배열)
    '''
    N = corpus.shape[0]

    if corpus.ndim == 1:
        one_hot = np.zeros((N, vocab_size), dtype=np.int32)
        for idx, word_id in enumerate(corpus):
            one_hot[idx, word_id] = 1

    elif corpus.ndim == 2:
        C = corpus.shape[1]
        one_hot = np.zeros((N, C, vocab_size), dtype=np.int32)
        for idx_0, word_ids in enumerate(corpus):
            for idx_1, word_id in enumerate(word_ids):
                one_hot[idx_0, idx_1, word_id] = 1

    return one_hot
```

### 데이터 준비 전체
```python
import sys
sys.path.append('../')
from common.util import preprocess, create_contexts_target, convert_one_hot

text = 'You say goodbye and I say hello.'
corpus, word_to_id, id_to_word = preprocess(text)

contexts, target = create_contexts_target(corpus, window_size=1)

vocab_size = len(word_to_id)
target = convert_one_hot(target, vocab_size)
contexts = convert_one_hot(contexts, vocab_size)
```
