---
aliases:
  - Pooling Layer
  - 풀링 계층(Pooling Layer)
---

- 세로 · 가로 방향의 공간을 줄이는 [[연산]] 계층
- [[합성곱 계층]] 뒤에 [[배치 처리 1|배치]] -> 공간 크기 축소

### 종류
- 최대 풀링 (max pooling) : 특정 크기 영역에서 가장 큰 원소 선택
	- [[스트라이드]] = 영역 크기
	- 이미지 인식에서 주로 사용
- 평균 풀링 (average pooling) : 대상 영역의 평균 계산
- ==ex)== 2 × 2 최대 풀링 (스트라이드 2)
![[합성곱-신경망-(CNN)-20.png]]

### 특징
- 학습할 매개변수 X -> 최댓값 · 평균 취하는 처리뿐
- 채널 수 불변 -> 채널마다 독립 계산
![[합성곱-신경망-(CNN)-21.png]]
- 입력 변화에 강건 -> 입력 [[데이터]] 조금 변해도 결과 유사
![[합성곱-신경망-(CNN)-22.png]]
	- 한 칸씩 밀려도 같은 결과

### 구현
- [[im2col]] 로 입력 전개, 단 채널 쪽 독립
![[합성곱-신경망-(CNN)-29.png]]
![[합성곱-신경망-(CNN)-30.png]]
1. 찾을 구역 (2 × 2) 을 한 줄로 배치
2. 각 줄에서 최댓값 [[Searching|탐색]]
3. `reshape` 로 형상 복원

```python
class Pooling:
    def __init__(self, pool_h, pool_w, stride=2, pad=0):
        self.pool_h = pool_h
        self.pool_w = pool_w
        self.stride = stride
        self.pad = pad

    def forward(self, x):
        N, C, H, W = x.shape
        out_h = int(1 + (H - self.pool_h) / self.stride)
        out_w = int(1 + (W - self.pool_w) / self.stride)

        # 펼치기
        col = im2col(x, self.pool_h, self.pool_w, self.stride, self.pad)
        col = col.reshape(-1, self.pool_h*self.pool_w)

        # 최댓값 찾기
        out = np.max(col, axis=1)

        # reshape
        out = out.reshape(N, out_h, out_w, C).transpose(0, 3, 1, 2)

        return out
```

### 역전파
- [[ReLU 계층]] 의 max [[역전파]] 참고
```python
def backward(self, dout):
    dout = dout.transpose(0, 2, 3, 1)

    pool_size = self.pool_h * self.pool_w
    dmax = np.zeros((dout.size, pool_size))
    dmax[np.arange(self.arg_max.size), self.arg_max.flatten()] = dout.flatten()
    dmax = dmax.reshape(dout.shape + (pool_size,))

    dcol = dmax.reshape(dmax.shape[0] * dmax.shape[1] * dmax.shape[2], -1)
    dx = col2im(dcol, self.x.shape, self.pool_h, self.pool_w, self.stride, self.pad)

    return dx
```
