---
aliases:
  - Learning Rate
  - 학습률(Learning Rate)
---

- [[경사 하강법]] 에서 매개변수 갱신량 결정하는 값 -> 기호 $\eta$ (eta)
- 미리 특정 값으로 설정 (0.01, 0.001 등) -> 변경하며 올바르게 학습 중인지 확인

### 특징
```python
# 학습률이 큰 경우
init_x = np.array([-3.0, 4.0])
print(gradient_descent(function_2, init_x=init_x, lr=10.0, step_num=100))

# 학습률이 작은 경우
init_x = np.array([-3.0, 4.0])
print(gradient_descent(function_2, init_x=init_x, lr=1e-10, step_num=100))
```
![[신경망-학습-17.png]]
- 크면 -> 큰 값으로 발산
- 작으면 -> 거의 갱신 안 된 채 종료

- 사람이 직접 설정 -> [[하이퍼 파라미터]]
