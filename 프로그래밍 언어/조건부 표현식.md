---
aliases:
  - Conditional Expressions
  - 조건부 표현식(Conditional Expressions)
---

- 조건 따라 값 하나 고르는 표현식 -> 대입문에 그대로 사용
- `if-then-else` [[제어문]]이 대입 자리에 오는 형태
- [[C]] : [[삼항 연산자]] `?` 로 제공

```c
average = (count == 0) ? 0 : sum/count ;
```
